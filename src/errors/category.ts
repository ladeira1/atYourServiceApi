// eslint-disable-next-line no-shadow
export enum CategoryErrors {
  NAME_ALREADY_IN_USE = 'CATEGORY.NAME_ALREADY_IN_USE',
  NOT_FOUND = 'CATEGORY.NOT_FOUND',
  REQUIRED_NAME = 'CATEGORY.REQUIRED_NAME',
}
